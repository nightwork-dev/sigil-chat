# Dependencies
node_modules
packages/chat-overlay/files
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
dist
.output
.nitro
.tanstack
.vinxi
.eve
.data

# Debug
npm-debug.log*

# Generated
**/routeTree.gen.ts

# GZ.1 gaze-spike runtime assets. Generated by apps/web/scripts/setup-gaze-assets.mjs.
apps/web/public/models/face_landmarker.task
apps/web/public/mediapipe/wasm/
# shadcn registry build output (regenerate: pnpm --filter @workspace/ui registry:build)
apps/web/public/r/
packages/ui/.registry-staging/

# Sigil CLI render temp dir
.sigil-render

# Reports are generated/local. Not tracked — no exceptions.
reports/

# Misc
.DS_Store
*.pem

# Agent tooling scratch state — .agents/index.md is the canonical instructions
# file (CLAUDE.md/AGENTS.md symlink to it), keep it; .agents/skills/ is the
# tracked, portable (codex-facing) skills tree, keep it too; ignore everything
# else under .agents (session memory, etc.)
.agents/*
!.agents/index.md
!.agents/skills/
.omc
.playwright-mcp
traces/
apps/web/public/llms.txt

# Internal planning/strategy docs — not shipped in the public template
docs.local/

# Machine-local agent coordination state (worktree map, roadmap store path,
# live agent roster) — loads for Claude Code on this machine but is never
# committed; see .claude/skills/local-coordination/SKILL.md.
.claude/skills/local-coordination/

# Local-only files/dirs (naming convention) — gitignored everywhere EXCEPT
# repos that are themselves local-only (e.g. the .sigil roadmap store).
*.local
*.local.*
