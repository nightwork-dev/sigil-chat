registry=https://registry.npmjs.org

# Interim: @zigil/agent-react@0.1.3 is published only to the local Verdaccio
# (public npm tops out at 0.1.1; the public npm token is expired — publish is a
# David follow-up). Everything else resolves from public npm per main.
@zigil:registry=http://localhost:4873
