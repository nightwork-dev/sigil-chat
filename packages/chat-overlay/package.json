{
  "name": "@sigil-design/chat-overlay",
  "version": "0.1.2",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "Sigil Chat application overlay for the Sigil Design scaffold.",
  "files": [
    "overlay.sigil.json",
    "files",
    "README.md"
  ],
  "scripts": {
    "stage": "node scripts/stage-overlay.mjs",
    "registry:item": "node scripts/emit-registry-item.mjs",
    "test": "vitest run",
    "test:registry-smoke": "node scripts/registry-smoke.mjs",
    "check:import-closure": "node scripts/check-import-closure.mjs",
    "prepack": "pnpm stage",
    "postpack": "node scripts/clean-overlay.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=24"
  }
}
